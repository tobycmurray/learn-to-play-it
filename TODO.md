# TODO

## GUI App Packaging
- Package as a macOS `.app` bundle (PyInstaller, py2app, or Briefcase)
- Set `CFBundleName` to "Learn To Play It" in `Info.plist` (fixes "Python" in menu bar)
- Set `CFBundleIconFile` with a custom icon (replaces rocket ship in Dock/app switcher)
- Goal: standalone downloadable app for non-technical users

## GUI Polish
- Improve visual styling of the player interface
- Delete `FUTURE.md` (all items implemented)
